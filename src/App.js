import React, { useRef, useEffect } from "react";

const App = () => {

  // const mapRef = useRef()

  // useEffect(() => {
    
  // })

  return (
    <div className="webmap"> 
      <h1>Hello World!</h1>
    </div>
  );
}

export default App